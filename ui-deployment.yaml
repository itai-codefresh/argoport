apiVersion: apps/v1
kind: Deployment
metadata:
  name: argoport-ui
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: argoport-ui
    spec:
      containers:
        - name: argoport-ui
          image: itaicodefresh/argoport-ui:main
          ports:
            - containerPort: 4200
